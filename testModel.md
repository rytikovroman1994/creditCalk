# Тестова модель

## Общие условия тестирования

- Функция должна возвращать ```'Статус выдачи кредина': false```, если возраст превышает пенсионный возраст на момент возврата кредита.
- Функция должна возвращать ```'Статус выдачи кредина': false```, если результат деления запрошенной суммы на срок погашения в годах более трети годового дохода.
- Функция должна возвращать ```'Статус выдачи кредина': false```, если кредитный рейтинг -2.
- Функция должна возвращать ```'Статус выдачи кредина': false```, если в источнике дохода указано "безработный".
- Функция должна возвращать ```'Статус выдачи кредина': false```, если годовой платёж (включая проценты) больше половины дохода. 

В остальных случаях функция должна возвращать ```{ 'Статус выдачи кредина': true, 'Сумма ежегодных выплат': ${sum} }```

## Тест кейсы

### Проверка по пенсионному возрасту
1. Проверить что Функция проверки возраста возвращает ```'Статус выдачи кредина': false``` если возраст до 18 лет, не включительно. 
2. Проверить что Функция проверки возраста возвращает```'Статус выдачи кредина': false``` если возраст + срок погашения кредита превышает пенсионный. Для мужчин 65, для женщин 60. 
3. Проверить что Функция проверки возраста возвращает ERROR при входящих данных - возраст отрицатпельный.
4. Проверить что Функция проверки возраста возвращает ERROR при входящих данных - не man или не woman.
5. Проверить что Функция проверки возраста отрабатывает коректо при входящих данных - Man, Woman.
6. Проверить что Функция проверки возраста отрабатывает коректо при пограничных возрастных цифрах. 


### Проверка по годовому доходу
1. Проверить что Функция проверки по годовому доходу возвращает ```'Статус выдачи кредина': false``` если 1/3 годового дохода меньше запрошенной суммы делённой на срок погашения кредита. 
2. Проверить что Функция проверки по годовому доходу возвращает ```'Статус выдачи кредина': true``` если 1/3 годового дохода больше запрошенной суммы делённой на срок погашения кредита.

### Проверка по кредитному рейтингу
1. Проверить что Функция проверки по кредитному рейтингу возвращает ERROR в случаи если входящие данные не из массива [-2,-1,0,1,2];
2. Проверить что Функция проверки по кредитному рейтингу возвращает ```'Статус выдачи кредина': false``` если кредитный рейтинг  [-2];
3. Проверить что Функция проверки по кредитному рейтингу возвращает ```'Статус выдачи кредина': true``` если кредитный рейтинг  [-1,0,1,2];

### Проверка по статусу работы
1. Проверить что Функция проверки по статусу работы возвращает ERROR в случаи если входящие данные не из массива ['безработный',
'пассивный доход','наёмный работник','собственный бизнес'];
2. Проверить что Функция проверки по статусу работы возвращает ```'Статус выдачи кредина': false``` при условии, что человек безработный.
3. Проверить что Функция проверки по статусу работы возвращает ```'Статус выдачи кредина': true``` при остальных условиях, доступных из селекта. 
4. Проверить что функция корректно отрабатывает при уловии, что входящие данные могут вводится в загловной буквы, или в верхнем регистре. 

### Получение суммы по виду дохода
1. Проверить что Функция получение суммы по виду дохода возвращает ERROR в случаи если входящие данные не содержат ['пассивный доход','наёмный работник','собственный бизнес'];
2. Проверить что Функция получение суммы по виду дохода возвращает ```1``` при пассивный доход.
3. Проверить что Функция получение суммы по виду дохода возвращает ```5``` при наёмный работник.
4. Проверить что Функция получение суммы по виду дохода возвращает ```10``` при собственный бизнес.
5. Проверить что функция корректно отрабатывает при уловии, что входящие данные могут вводится в загловной буквы, или в верхнем регистре.

### Получаем суммы по кредитному рейтингу
1. Проверить что Функция получение суммы по кредитному рейтингу возвращает ERROR в случаи если входящие данные не содержат [-1,0,1,2];
2. Проверить что Функция получение суммы по кредитному рейтингу возвращает ```1``` при рейтинге -1;
3. Проверить что Функция получение суммы по кредитному рейтингу возвращает ```5``` при рейтинге 0;
4. Проверить что Функция получение суммы по кредитному рейтингу возвращает ```10``` при рейтинге 1;
5. Проверить что Функция получение суммы по кредитному рейтингу возвращает ```10``` при рейтинге 2;

### Проверка получения суммы кредита 
Функция принимает в аргументы результат двух предыдущих функций и должна отдавать меньший результат.
Каждая из функций может вернуть значение в диапазоне [1,5,10].
1. Проверить в разных вариациях, что функция будет возвращать всегда меньшее значение. 

### Модификаторы по источникам дохода
1. Модификаторы по источнику дохода принимают данные и отдаёт результаты согласно правилу -
[ипотека: -2, развитие бизнеса: -0.5, потребительский: 1.5]
### Модификаторы по кредитному рейтингу
1. Модификаторы по кредитному рейтингу принимают данные и отдаёт результаты согласно правилу -
[-1: 1.5, 0: 0, 1: -0.25, 2: -0.75]
### Модификаторы по источнику дохода
1. Модификаторы по источнику дохода принимают данные и отдаёт результаты согласно правилу -
[пассивный доход: 0.5, наёмный работник: -0.25, собственный бизнес: 0.25]
### Модификаторы по сумме кредита 
1. Рассчитывается по формуле  ```-Math.log10(income)```

### Получение модификатора процентной ставки
Функция принимает несколько значений:
1. Источник дохода 
2. Сумма кредита
3. Кредитный рейтинг
4. Причина по которой совершается заём

1. Воспроизвести логику вычисления общей суммы модификатора
2. В цикле от 100 едениц генерировать данные каждого типа модикатора 
3. Вызывать функцию модификатора процентной ставки и сранвить результаты 
4. В случаи ошибки, выводить данные, которые применялись для тестирования

### Получаем сумму ежегодного платежа
Сумма расчитывается по формуле ```(<сумма кредита> * (1 + <срок погашения> * (<базовая ставка> + <модификаторы>))) / <срок погашения>```
В случаи если заправшиваемая сумма кредита меньше, чем максимально разрешённая по кретериям сумма, то сумма расчитывается исходя из запрошенно суммы. 
В случаи если заправшиваемая сумма кредита больше, чем максимально разрешённая по кретериям сумма, то сумма расчитывается исходя из максимально разрешённой по кретериям суммы.

1. Проверить на реальных кейсах отработку функции, что исходя из заданных данных она возвращает правильную сумму кредита. 
2. В случаи ошибки, выводить данные, которые применялись для тестирования.

### Проверка суммы ежегодного платежа по отношению к половине ежегодного дохода
1. В цикле не менее 100 едениц проверить работу функции на сгенерируемых данных.
2. Проверить работу функции на всех возможных комбинациях модификаторв.
3.  В случаи ошибки, выводить данные, которые применялись для тестирования.

### Проверка функции на возможность выдачи кредита 
Функция которая принимает пять значений, которые могут быть ```true```|| ```false```. 
1. Проверить, что если одно из значений входных данных равно ```false``` то кредит не выдаётся. 

### Проверка главной функции 
1. Проверить на реальных кейсах отработку функции, что исходя из заданных данных она возвращает правильный результат.
2. Интеграционные тесты с генерируемыми данными для вызова вызова функций, которые вернут нужные значения для выполения основной функции. 
3. В случаи если кредит разрешён, функция возвращает сумму ежегодного платежа. 
4. В случаи если кредит не разрешён, возвращает ```false```
